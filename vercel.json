{
  "buildCommand": "cd documentation && bun install --frozen-lockfile && bun run build",
  "outputDirectory": "documentation/build",
  "devCommand": "cd documentation && bun start",
  "installCommand": "cd documentation && bun install --frozen-lockfile"
}
